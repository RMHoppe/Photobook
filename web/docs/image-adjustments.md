# Adjusting Images

After placing an image in a frame you can reposition, scale, and rotate it without changing the layout. These adjustments affect only how the image sits within its frame — the frame itself and the surrounding layout are unchanged.

## Table of Contents

- [Panning an image](#panning-an-image)
- [Scaling an image](#scaling-an-image)
- [Rotating an image](#rotating-an-image)
- [Swapping two images](#swapping-two-images)

## Panning an image

**Drag** inside a frame to pan the image within it. The image is always kept large enough to fully cover the frame — panning is constrained so no background shows through.

## Scaling an image

**Scroll** the mouse wheel over a frame to zoom the image in or out. The minimum scale is 1.0 (image exactly covers the frame); there is no maximum.

## Rotating an image

Hold **Shift** and **scroll** the mouse wheel over a frame to rotate the image within it. Each scroll step rotates by 0.5°.

## Swapping two images

There are two ways to swap images between frames:

**Toolbar button:** Click the **Swap** button (arrows icon) in the canvas toolbar to activate swap mode. The button stays highlighted while the tool is active. Then drag from one frame to another. Click the button again or press **Escape** to deactivate.

**Alt key:** Hold **Alt** and drag from one frame to another without activating the toolbar button.

In both cases, the source and target frames are highlighted while dragging. Release over the target frame to complete the swap; release outside any frame to cancel.
